import { AccNumberValidatorDirective } from './acc-number-validator.directive';

describe('AccNumberValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AccNumberValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
